---
title: "Optimum-location uncertainty and near-optimal regions"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Optimum-location uncertainty and near-optimal regions}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(collapse = TRUE, comment = "#>")
```

```{r include-extended, results='asis', echo=FALSE}
src <- system.file("monographs", "
29-optimum-location-uncertainty-and-near-optimal-regions.md
", package = "mixRSMflow")
if (!nzchar(src)) {
  src <- file.path("inst", "monographs", "
29-optimum-location-uncertainty-and-near-optimal-regions.md
")
  if (!file.exists(src)) src <- ""
}
if (nzchar(src)) {
  cat(readLines(src, warn = FALSE, encoding = "UTF-8"), sep = "\n")
} else {
  cat("**Extended tutorial not found.**\n")
}
```
