---
title: "Generalized, hierarchical and Bayesian mixture models"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Generalized, hierarchical and Bayesian mixture models}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(collapse = TRUE, comment = "#>")
```

```{r include-extended, results='asis', echo=FALSE}
src <- system.file("monographs", "
31-generalized-hierarchical-and-bayesian-mixture-models.md
", package = "mixRSMflow")
if (!nzchar(src)) {
  src <- file.path("inst", "monographs", "
31-generalized-hierarchical-and-bayesian-mixture-models.md
")
  if (!file.exists(src)) src <- ""
}
if (nzchar(src)) {
  cat(readLines(src, warn = FALSE, encoding = "UTF-8"), sep = "\n")
} else {
  cat("**Extended tutorial not found.**\n")
}
```
